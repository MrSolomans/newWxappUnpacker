<view bindtouchmove="viewTouchMove" catch:tap="showBorder" class="circle {{detectBg}}" style="left:{{left}}px;top:{{top}}px;" wx:if="{{isShowDetect}}">
    <view class="components-num detectBorder">{{componentsAry.length>0?componentsAry.length+'个组件':'组件检测'}}</view>
</view>
