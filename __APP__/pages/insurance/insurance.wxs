var filter = ({
    tagList: ['1', '3', '7'],
    getType: (function(type, str) {
        var reg = getRegExp(type).test(str);
        return (reg)
    }),
    filterTag: (function(tagName, productTag) {
        return (!!tagName && filter.tagList.indexOf('' + productTag) === -1)
    }),
    filterNull: (function(arr) {
        var newArr = [];
        arr.forEach((function(value) {
            if (value) {
                newArr.push(value)
            }
        }));
        return (newArr)
    }),
    filterNum: (function(num) {
        var ratio = 10000;
        return (num >= ratio ? parseInt(num / ratio) + '万' : num)
    }),
});
module.exports = filter;